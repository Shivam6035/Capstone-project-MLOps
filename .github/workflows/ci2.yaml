# name: Deploy to EKS

# on:
#   push:
#     branches: [ main ]

# jobs:
#   deploy:
#     # This forces the job to execute on your EC2 instance
#     runs-on: self-hosted 

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Build Docker Image
#         run: |
#           docker build -t my-flask-app:latest .
#           # (Add your ECR push steps here)

#       - name: Deploy to EKS
#         run: |
#           aws eks update-kubeconfig --region us-east-1 --name flask-app-cluster
#           kubectl apply -f k8s/deployment.yaml